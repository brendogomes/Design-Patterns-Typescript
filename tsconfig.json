{
  "compilerOptions": {
    // Versão do ecmascript
    "target": "ES2022",
    // Sistema de modulos do CommonJS "Node"
    "module": "CommonJS",
    //  Libs utilizadas: DOM (console.log)
    "lib": ["DOM", "ESNext"],
    //  Saída de compilação
    "outDir": "./dist",
    //  Raiz do projeto
    "rootDir": "./src",
    // Remove os comentários ao compilar
    "removeComments": true,
    // Não emite o código de erro após compilação
    "noEmitOnError": false,
    // Modo mais estrito do TS
    "strict": true,
    "esModuleInterop": true,
    // Pula a checagem de arquivos como .ts para acelerar a compilação do projeto
    "skipLibCheck": true,
    // Não irá permitir nomes inconsistentes de arquivos
    "forceConsistentCasingInFileNames": true
  },
  "include": ["./src"] 
}
